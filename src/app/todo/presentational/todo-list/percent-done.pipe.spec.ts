import { PercentDonePipe } from './percent-done.pipe';

describe('PercentDonePipe', () => {
  it('create an instance', () => {
    const pipe = new PercentDonePipe();
    expect(pipe).toBeTruthy();
  });
});
